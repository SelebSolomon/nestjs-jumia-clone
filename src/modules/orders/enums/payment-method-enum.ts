export enum PaymentMethod {
  CARD = 'card',
  PAYPAL = 'paypal',
  CRYPTO = 'crypto',
  CASH = 'cash',
}

// export const paymentMethods = ['card', 'paypal', 'crypto', 'cash'] as const;

// export type PaymentMethod = (typeof paymentMethods)[number];
