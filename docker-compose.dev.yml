services:
  mongo:
    ports:
      - "27017:27017"   # 👈 Compass access

  api:
    command: npm run start:dev
    ports:
      - "3000:3000"
    env_file:
      - .env.development
    volumes:
      - .:/app
      - /app/node_modules
